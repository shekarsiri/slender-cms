<?php

return array(
	'modules'	=> array(
		'user' => array(),		
		'site' => array(
				'name' 	=> '',
				'url'	=> '',
			),
		'page' => array(
				// title
				// meta.title
				// meta.keywords
				// body
				// slug (should default to sluggified title, but can be set manually)
				// availability.sunrise
				// availability.sunset
				// created
				// updated
			),
		// 'video' => array(
		// 	),

		),
);